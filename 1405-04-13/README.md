# PYTHON MASTER CHALLENGES

مهلت ارسال: 1405-04-19 (جمعه) | ترجیحا داخل یک فایل (`*.ipynb`)

---

1. برنامه‌ای بنویسید که بدون استفاده از str[::-1] یک رشته را برعکس کند.

2. تعداد حروف بزرگ، حروف کوچک، اعداد و کاراکترهای خاص یک رشته را محاسبه کنید.

3. بدون استفاده از count() تعداد تکرار هر کاراکتر را پیدا کنید.

4. بررسی کنید آیا دو رشته Anagram هستند یا خیر.

5. بدون استفاده از sorted() بررسی کنید آیا دو رشته شامل حروف یکسان هستند.

6. طولانی‌ترین کلمه یک جمله را پیدا کنید.

7. اولین کاراکتری که فقط یک بار تکرار شده را پیدا کنید.

8. اولین کاراکتری که بیش از یک بار تکرار شده را پیدا کنید.

9. بدون استفاده از split() رشته را به کلمات تقسیم کنید.

10. بدون استفاده از join() لیستی از کلمات را به رشته تبدیل کنید.

11. متد replace() را خودتان پیاده‌سازی کنید.

12. متد strip() را بدون استفاده از strip() پیاده‌سازی کنید.

13. متد startswith() را خودتان بنویسید.

14. متد endswith() را خودتان بنویسید.

15. بررسی کنید رشته Palindrome است یا خیر.

16. تمام زیررشته‌های ممکن یک رشته را تولید کنید.

17. بزرگ‌ترین زیررشته بدون کاراکتر تکراری را پیدا کنید.

18. تعداد تمام حروف صدادار را محاسبه کنید.

19. تمام جایگشت‌های یک رشته را تولید کنید.

20. بررسی کنید رشته فقط شامل اعداد است یا خیر، بدون isnumeric().

21. دومین عدد بزرگ یک لیست را پیدا کنید.

22. سومین عدد کوچک یک لیست را پیدا کنید.

23. لیست را بدون sort() مرتب کنید (Bubble Sort).

24. لیست را با Selection Sort مرتب کنید.

25. لیست را با Insertion Sort مرتب کنید.

26. بدون استفاده از max() بزرگ‌ترین مقدار را پیدا کنید.

27. بدون استفاده از min() کوچک‌ترین مقدار را پیدا کنید.

28. عناصر تکراری لیست را حذف کنید بدون تبدیل به set.

29. تعداد تکرار هر عنصر لیست را محاسبه کنید.

30. دو لیست را بدون zip() با هم ترکیب کنید.

31. اشتراک دو لیست را بدون set پیدا کنید.

32. اجتماع دو لیست را بدون set پیدا کنید.

33. اختلاف دو لیست را بدون set پیدا کنید.

34. بررسی کنید آیا یک لیست زیرمجموعه لیست دیگر است یا خیر.

35. لیست را فقط با استفاده از حلقه برعکس کنید.

36. عناصر زوج و فرد را جدا کنید.

37. عناصر مثبت، منفی و صفر را جدا کنید.

38. تمام اعداد اول موجود در لیست را پیدا کنید.

39. مجموع اعداد اول لیست را محاسبه کنید.

40. میانگین، میانه و مد یک لیست را بدون کتابخانه statistics محاسبه کنید.

41. جدول ضرب 1 تا 20 را چاپ کنید.

42. الگوی مثلثی ستاره‌ها را در چهار حالت مختلف چاپ کنید.

43. الگوی لوزی با ستاره چاپ کنید.

44. الگوی هرم توخالی چاپ کنید.

45. الگوی پروانه (Butterfly Pattern) چاپ کنید.

46. تمام اعداد Armstrong بین 1 تا 100000 را پیدا کنید.

47. تمام اعداد Perfect بین 1 تا 10000 را پیدا کنید.

48. تمام اعداد Strong Number را پیدا کنید.

49. بررسی کنید یک عدد Happy Number است یا خیر.

50. مسئله کلاسیک FizzBuzz را پیاده‌سازی کنید، اما به‌گونه‌ای که تعداد قوانین قابل تغییر و توسعه باشد (قابل افزودن قوانین جدید بدون تغییر منطق اصلی برنامه).

51. تابعی بنویسید که بدون استفاده از sum() مجموع عناصر یک لیست را محاسبه کند.

52. تابعی بنویسید که بدون استفاده از len() طول یک iterable را محاسبه کند.

53. تابعی بنویسید که مانند max() عمل کند.

54. تابعی بنویسید که مانند min() عمل کند.

55. تابعی بنویسید که مانند enumerate() عمل کند.

56. تابعی بنویسید که مانند zip() عمل کند.

57. تابعی بنویسید که مانند map() عمل کند.

58. تابعی بنویسید که مانند filter() عمل کند.

59. تابعی بنویسید که مانند any() عمل کند.

60. تابعی بنویسید که مانند all() عمل کند.

61. تابع بازگشتی برای محاسبه فاکتوریل بنویسید.

62. تابع بازگشتی برای محاسبه دنباله فیبوناچی بنویسید.

63. تابع بازگشتی برای برعکس کردن رشته بنویسید.

64. تابع بازگشتی برای محاسبه مجموع ارقام یک عدد بنویسید.

65. تابع بازگشتی برای جستجو در یک لیست بنویسید.

66. یک Decorator بنویسید که زمان اجرای هر تابع را اندازه‌گیری کند.

67. یک Decorator بنویسید که تعداد دفعات فراخوانی یک تابع را ثبت کند.

68. یک Decorator بنویسید که خروجی تابع را Cache کند.

69. یک Decorator بنویسید که فقط به کاربران Admin اجازه اجرای تابع را بدهد.

70. یک Decorator بنویسید که تمام آرگومان‌های ورودی را Log کند.

71. یک Generator برای تولید اعداد اول بنویسید.

72. یک Generator برای تولید اعداد فیبوناچی بنویسید.

73. یک Generator برای خواندن فایل‌های بزرگ به صورت خط‌به‌خط بنویسید.

74. یک Generator بنویسید که تمام جایگشت‌های ممکن یک رشته را تولید کند.

75. یک Generator بنویسید که اعداد زوج نامتناهی تولید کند.

76. با استفاده از Lambda لیستی از دیکشنری‌ها را بر اساس چند کلید مرتب کنید.

77. با استفاده از Lambda طولانی‌ترین رشته لیست را پیدا کنید.

78. با استفاده از Lambda تمام اعداد منفی را حذف کنید.

79. با استفاده از Lambda مربع تمام اعداد را تولید کنید.

80. چندین Lambda را به صورت زنجیره‌ای روی داده‌ها اعمال کنید.

81. یک فایل متنی را بخوانید و تعداد خطوط، کلمات و کاراکترها را محاسبه کنید.

82. پرتکرارترین کلمه یک فایل را پیدا کنید.

83. تمام خطوط تکراری فایل را حذف کنید.

84. دو فایل متنی را با هم مقایسه کنید و خطوط متفاوت را نمایش دهید.

85. فایل بسیار بزرگی را بدون بارگذاری کامل در حافظه پردازش کنید.

86. تمام فایل‌های یک پوشه را پیمایش کنید.

87. فایل‌هایی با پسوند مشخص را پیدا کنید.

88. پوشه‌ای را به صورت بازگشتی کپی کنید.

89. حجم تمام فایل‌های یک پوشه را محاسبه کنید.

90. تمام فایل‌های خالی یک پوشه را پیدا کنید.

91. یک فایل JSON را بخوانید و ساختار آن را اعتبارسنجی کنید.

92. فایل CSV را بخوانید و میانگین یک ستون عددی را محاسبه کنید.

93. فایل CSV را بر اساس یک ستون مرتب کنید.

94. فایل JSON را به CSV تبدیل کنید.

95. فایل CSV را به JSON تبدیل کنید.

96. یک Logger حرفه‌ای بسازید که هم در کنسول و هم در فایل Log بنویسد.

97. Logger را طوری تنظیم کنید که هر روز فایل Log جدید ایجاد شود.

98. تمام Exceptionهای برنامه را در فایل Log ذخیره کنید.

99. یک Context Manager سفارشی برای باز کردن فایل بنویسید.

100. یک Context Manager بنویسید که زمان اجرای هر بلوک with را اندازه‌گیری و ثبت کند.